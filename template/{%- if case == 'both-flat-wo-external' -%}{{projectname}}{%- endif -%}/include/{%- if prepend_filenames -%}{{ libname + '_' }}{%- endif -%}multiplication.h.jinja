#ifndef {% if prepend_filenames %}{{ (libname + '_') | upper }}{% endif %}MULTIPLICATION_H
#define {% if prepend_filenames %}{{ (libname + '_') | upper }}{% endif %}MULTIPLICATION_H
#include "{% if prepend_filenames %}{{ libname + '_' }}{% endif %}export.h"  // generated by CMake, defines {{ libname | upper }}_EXPORT

{{ libname | upper }}_EXPORT int {% if prepend_symbols %}{{ libname + '_' }}{% endif %}multiply (int, int);

#endif
