#ifndef {% if prepend_symbols %}{{ (libname + '_') | upper }}{% endif %}ADDITION_H
#define {% if prepend_symbols %}{{ (libname + '_') | upper }}{% endif %}ADDITION_H
#include "{{ libname }}/export.h"  // generated by CMake, defines {{ libname | upper }}_EXPORT

{{ (libname | upper) }}_EXPORT int {% if prepend_symbols %}{{ libname + '_' }}{% endif %}add (int, int);

#endif
