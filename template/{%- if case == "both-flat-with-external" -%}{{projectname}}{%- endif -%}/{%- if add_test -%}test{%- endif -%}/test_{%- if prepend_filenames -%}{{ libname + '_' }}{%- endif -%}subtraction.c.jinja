#include <criterion/criterion.h>
#include "{%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}subtraction.h"

Test({%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}subtraction, one){
    int actual = {%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}subtract(2, 3);
    int expected = -1;
    cr_assert(actual == expected);
}

Test({%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}subtraction, two){
    int actual = {%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}subtract(2, -3);
    int expected = 5;
    cr_assert(actual == expected);
}
