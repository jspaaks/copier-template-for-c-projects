#include <criterion/criterion.h>
#include "{%- if prepend_filenames -%}{{ libname + '_' }}{%- endif -%}addition.h"

Test({%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}addition, one){
    int actual = {%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}add(2, 3);
    int expected = 5;
    cr_assert(actual == expected);
}

Test({%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}addition, two){
    int actual = {%- if prepend_symbols -%}{{ libname + '_' }}{%- endif -%}add(2, -3);
    int expected = -1;
    cr_assert(actual == expected);
}
