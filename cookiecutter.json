{
  "__prompts__" : {
    "project_slug": "What is the name of the new project?\n        Use [A-Za-z0-9_-.]+",
    "produces": "What artifact(s) does the new project produce?",
    "exename": "If any, what is the name of the executable?\n         Use [A-Za-z0-9_-.]+",
    "libname": "If any, what is the name of the library?\n         Use lib[A-Za-z0-9_-.]+)",
    "nested": "Do you want to organize components into subdirectories under src/?",
    "build_directory": "What should the build directory be called?",
    "external_directory": "What should the directory that contains external dependencies be called?",
    "add_assets": "Does the project use assets like images, sound, fonts, etc?",
    "add_test": "Does the project use tests?",
    "add_cmake": "Add a CMake build configuration?",
    "add_codeblocks": "Add a Code::Blocks IDE configuration?",
    "add_clang_format": "Add a clang-format configuration?"
  },
  "project_slug": "calculator-project",
  "produces": [
    "an executable",
    "a library",
    "both"
  ],
  "exename": "{% if cookiecutter.produces == 'an executable' %}calculator{% elif cookiecutter.produces == 'a library' %}<Press enter to skip>{% elif cookiecutter.produces == 'both' %}calculator{% endif %}",
  "libname": "{% if cookiecutter.produces == 'an executable' %}<Press enter to skip>{% elif cookiecutter.produces == 'a library' %}libcalculator{% elif cookiecutter.produces == 'both' %}libcalculator{% endif %}",
  "nested": true,
  "build_directory": [
    "build",
    "built",
    "generated"
  ],
  "external_directory": [
    "external",
    "third_party",
    "lib",
    "submodules",
    "subprojects",
    "My project doesn't have external dependencies"
  ],
  "add_assets": true,
  "add_test": true,
  "add_cmake": true,
  "add_codeblocks": true,
  "add_clang_format": true
}
