{
  "__prompts__" : {
    "build_directory": "What should the build directory be called?",
    "builds_executable": "Does the new project build an executable?",
    "produces": "What artifact(s) does the new project produce?",
    "external_directory": "What should the directory that contains external dependencies be called?",
    "keep_assets": "Does the project use assets like images, sound, fonts, etc?",
    "project_slug": "What is the name of the new project?\n        Use [A-Za-z0-9_-.]+"
  },
  "produces": [
    "an executable",
    "a library",
    "both"
  ],
  "project_slug": "{% if cookiecutter.produces in ['an executable', 'both'] -%}calculator{%- else -%}liboperations{% endif %}",
  "build_directory": [
    "build",
    "built",
    "generated"
  ],
  "external_directory": [
    "external",
    "third_party",
    "lib",
    "submodules",
    "subprojects",
    "My project doesn't have external dependencies"
  ],
  "keep_assets": true
}
